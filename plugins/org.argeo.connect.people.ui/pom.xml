<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.argeo.connect</groupId>
		<artifactId>plugins</artifactId>
		<version>2.1.6-SNAPSHOT</version>
		<relativePath>..</relativePath>
	</parent>
	<artifactId>org.argeo.connect.people.ui</artifactId>
	<name>Connect People UI</name>
	<packaging>jar</packaging>
	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.felix</groupId>
				<artifactId>maven-bundle-plugin</artifactId>
				<configuration>
					<instructions>
						<Bundle-Activator>org.argeo.connect.people.ui.PeopleUiPlugin</Bundle-Activator>
						<Import-Package>
							*,
							org.argeo.connect.people,
							org.argeo.connect.media,
							org.argeo.eclipse.spring,
							org.springframework.dao,
							org.springframework.core
						</Import-Package>
					</instructions>
				</configuration>
			</plugin>
		</plugins>
	</build>
	<dependencies>

		<!-- RCP / RAP Specific bundles - Provided by the target plateform at runtime -->
		<!-- TODO clean this, this bundle contains many rap specific piece of code. 
			(among other RAP SPECIFIC DROPDOWN WIDGET) -->
		<dependency>
			<groupId>org.argeo.connect</groupId>
			<artifactId>org.argeo.connect.people.rap</artifactId>
			<version>2.1.6-SNAPSHOT</version>
			<!-- <scope>provided</scope> -->
		</dependency>

		<dependency>
			<groupId>org.argeo.tp.rap.addons</groupId>
			<artifactId>org.eclipse.rap.addons.dropdown</artifactId>
			<version>0.2.0.20131212-1229</version>
		</dependency>


		<!-- CONNECT -->
		<dependency>
			<groupId>org.argeo.connect</groupId>
			<artifactId>org.argeo.connect.people.services</artifactId>
			<version>2.1.6-SNAPSHOT</version>
		</dependency>

		<!-- People model -->
		<dependency>
			<groupId>org.argeo.connect</groupId>
			<artifactId>org.argeo.connect.people.core</artifactId>
			<version>2.1.6-SNAPSHOT</version>
		</dependency>

		<!-- COMMONS -->
		<!-- Base Argeo Eclipse Utilities -->
		<dependency>
			<groupId>org.argeo.commons.base</groupId>
			<artifactId>org.argeo.eclipse.ui</artifactId>
			<version>${version.argeo-commons}</version>
		</dependency>

		<!-- RAP only dependency, needed at compile time -->
		<dependency>
			<groupId>org.argeo.commons.base</groupId>
			<artifactId>org.argeo.eclipse.ui.rap</artifactId>
			<version>${version.argeo-commons}</version>
			<scope>provided</scope>
		</dependency>

		<dependency>
			<groupId>org.argeo.commons.base</groupId>
			<artifactId>org.argeo.eclipse.ui.jcr</artifactId>
			<version>${version.argeo-commons}</version>
		</dependency>

		<dependency>
			<groupId>org.argeo.commons.server</groupId>
			<artifactId>org.argeo.server.jcr</artifactId>
			<version>${version.argeo-commons}</version>
		</dependency>

		<!-- Extended by People to provide business specific user management -->
		<dependency>
			<groupId>org.argeo.commons.security</groupId>
			<artifactId>org.argeo.security.ui.admin</artifactId>
			<version>${version.argeo-commons}</version>
		</dependency>

		<!-- THIRD PARTIES -->
		<!-- Calc exports -->
		<dependency>
			<groupId>org.argeo.tp</groupId>
			<artifactId>jxl</artifactId>
		</dependency>
	</dependencies>
</project>