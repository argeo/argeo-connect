package org.argeo.connect.people.imports;

import org.apache.commons.logging.Log;
import org.apache.commons.logging.LogFactory;
import org.argeo.support.junit.AbstractSpringTestCase;

public class SimpleTest extends AbstractSpringTestCase {
	private final static Log log = LogFactory.getLog(SimpleTest.class);

	public void testIt() throws Exception {
		log.trace("Implement here a real test");
	}
}